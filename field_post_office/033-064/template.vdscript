declare source_dir;
source_dir = "$$$dir$$$";

declare output_dir;
output_dir = "K:\\Media\\Video\\tmp\\";

VirtualDub.Open(source_dir + "$$$clip$$$r.avs");
VirtualDub.video.SetMode(0);
VirtualDub.SaveAVI(output_dir + "$$$clip$$$cf.avi");
